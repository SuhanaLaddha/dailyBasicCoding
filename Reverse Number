#include<bits/stdc++.h>
using namespace std;
void reverse(int n){
    int rev=0;
    int sum=0;
    while(n>0){
        rev=n%10;
        sum=sum*10+rev;
        n=n/10;
    }
    cout<<sum;
}
int main(){
    int n;
    cin>>n;
    reverse(n);
    return 0;
}
