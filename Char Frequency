#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin>>s;
    int n=s.length();
    // vector<int> freq(26,0);
    // for(char ch:s){
    //     freq[ch-'a']++;
    // } 
    // char k;
    // cin>>k;
    // cout<<freq[k-'a'];


    //Using unordered_map
    unordered_map<char,int>freq;
    for(char ch:s){
        freq[ch]++;
    }
    for(auto it:freq){
        cout<<it.first<<"->"<<it.second<<endl;
    }
    return 0;
}
