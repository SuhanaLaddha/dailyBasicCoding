#include <bits/stdc++.h>
using namespace std;
int solve(int n){
    if(n<=1)return n;
    return solve(n-1)+solve(n-2);
    
}
int main() {
    int n;
    cin >> n;
    cout<<solve(n);

    //If want ot print the series:
    // for(int i=0;i<=n;i++){
    //     cout<<solve(i)<<" ";
    // }

    // int a = 0, b = 1;

    // for(int i = 0; i < n; i++) {
    //     cout << a << " ";

    //     int c = a + b;
    //     a = b;
    //     b = c;
    // }
    

    return 0;
}
